function Header() {
    return (
        <header>
            <nav className="navbar">
                <a href="">Login</a>
                <a href="">Register</a>
            </nav>
        </header>
    );
}

export default Header;